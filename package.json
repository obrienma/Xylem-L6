{
  "name": "xylem-l6",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone TypeScript stream processor for SaaS API activity logs — sliding-window security signals (velocity, first-seen, impossible travel, scope escalation).",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
